# Example keystore.properties (Do NOT commit your real credentials or keystore)
# Copy this to keystore.properties and set the real values:
#
# storeFile=/absolute/path/to/motolog-upload-key.jks
# storePassword=your_keystore_password
# keyAlias=motolog-upload
# keyPassword=your_key_password
